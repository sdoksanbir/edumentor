/**
 * Admin table primitives – ortak tipografi standardı.
 * Re-export from table (which applies admin standards):
 * - Header: text-xs font-medium tracking-wide text-muted-foreground uppercase
 * - Body: text-sm font-normal text-foreground
 * - Padding: px-4 py-3
 */
export { Table, TableHead, TableRow, TableCell } from "./table"
